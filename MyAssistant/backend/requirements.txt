Flask>=2.0
requests>=2.25
PyPDF2>=3.0.0
python-docx>=1.0.0
Pillow>=9.0.0
pytesseract>=0.3.8
chromadb>=0.4.0
sentence-transformers>=2.2.0
# Add other specific versions if necessary
# For example, if you need a specific version of Ollama client library (if one is used directly)
# ollama-python-client (example, if such a library exists and is used)
